
/// A simple controller class to hold user profile information,
/// including the user's name and phone number.
class ProfileController {
  ProfileController({required this.userName, required this.phoneNumber});
  String userName;
  String phoneNumber;
}
